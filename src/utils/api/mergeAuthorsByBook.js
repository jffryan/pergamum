const mergeAuthorsByBook = (bookSet) => {
  // This is going to be messy, but let's get it to work and we'll find a more elegant solution in the future.
  for (let i = 0; i < bookSet.length; i++) {
    // let currentBook = bookSet[i];
  }
};

export default mergeAuthorsByBook;
